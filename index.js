function getComputerPlay() {
    let random = Math.random() * 1;
    let rock = "Rock";
    let paper = "Paper";
    let scissor = "Scissor";

    if (random => 0.33333) {
        return rock;
    } else if (random => 0.66666) {
        return paper;
    } else {
        return scissor;
    }
}

